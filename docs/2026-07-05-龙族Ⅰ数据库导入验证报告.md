# 《龙族Ⅰ-火之晨曦》数据库导入验证报告

## 验证对象

- 源文件：`E:\books\龙族ⅰ-火之晨曦.txt`
- 数据库：`moqi_dev`
- 作品标题：`龙族Ⅰ-火之晨曦`
- 对应 issue：`DuGNan/Moqi#22`

## 验证命令

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\import-longzu-i.ps1 -SourcePath 'E:\books\龙族ⅰ-火之晨曦.txt'
powershell -ExecutionPolicy Bypass -File .\scripts\verify-longzu-i-import.ps1 -SourcePath 'E:\books\龙族ⅰ-火之晨曦.txt'
```

## 验证结果

- Flyway 已成功应用 `V8__add_story_knowledge_layer_tables.sql`。
- 源文件被解析为 13 个章节块。
- 数据库中 `chapters` 按 `chapter_no` 可完整还原标题、章节类型和正文。
- `chapters.chapter_type` 已覆盖献词、序幕、正文、尾声结构。
- 每个章节块均同步写入 `chapter_outlines`、`chapter_briefs`、`chapter_generations`，用于验证 P0 章节主流程表可承载既有小说导入后的结构化记录和原文生成稿。
- 完整性验证要求每个章节都有大纲、简报和导入稿，且 `chapter_generations.generated_content` 与 `chapters.content` 逐章一致。
- 本次导入不把小说原文提交到 Git，只写入本地 MySQL。

## 章节拆解与知识层记录统计

| 表 | 记录数 |
| --- | ---: |
| `chapter_outlines` | 13 |
| `chapter_briefs` | 13 |
| `chapter_generations` | 13 |
| `setting_candidates` | 10 |
| `setting_entries` | 10 |
| `foreshadowing_items` | 4 |
| `chapter_summaries` | 13 |
| `chapter_key_events` | 13 |

## 结论

当前数据库可以完整保存并按章节顺序还原《龙族Ⅰ-火之晨曦》的标题、章节类型和正文。`chapters.content` 是还原正文的唯一事实源，`chapter_generations.generated_content` 保存同章节原文副本，用于验证既有小说可以落入 P0 的“章节生成结果”链路。

P0 章节主流程表可以承载章节大纲、章节简报和导入稿记录；新增知识层表可以承载主要人物、组织、规则、地点、伏笔、章节摘要和关键事件，已经具备后续 AI 上下文检索的基础结构。

当前知识层记录属于验证导入用的人工规则辅助抽取结果，不等同于完整 AI 自动抽取能力。后续如需提高长篇理解完整度，应继续补充角色状态快照、叙事线程、时间线事件和知识巡检结果。
