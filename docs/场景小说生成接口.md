# 场景小说生成接口

## 适用范围

本接口以已发布的章节场景规划为唯一生成依据，创建的始终是可追溯、可取消、可重试的场景正文候选；不会直接写入 `chapters.content`，也不会修改设定、伏笔、章节摘要或关键事件。

## 创建与查询

- `POST /api/chapters/{chapterId}/generations`：创建批次。请求必须给出 `idempotencyKey` 和 `selectionMode`。`all` 生成全部已规划场景；`continue_from` 需要 `fromSceneKey`；`rewrite_selected` 需要非空 `sceneKeys` 和可复用的 `baseGenerationId`。
- `GET /api/generations/{generationId}/scenes`：按场景计划顺序读取候选清单。
- `GET /api/generations/{generationId}/scenes/{sceneId}`：读取单个候选的状态、上下文快照、模板版本、调用指标与来源候选引用；正文通过原生成详情读取，SSE 只通知资源变化。
- `POST /api/generations/{generationId}/cancel`：取消仍在排队或运行的批次，并传播给 Agent Run 与 Provider 流。
- `POST /api/generations/{generationId}/scenes/{sceneId}/retry`：基于预期尝试次数重试单场景的失败步骤。
- `POST /api/generations/{generationId}/regenerate`：基于路径中的原批次创建新的 `continue_from` 或 `rewrite_selected` 批次。请求中的 `baseGenerationId` 若提供，必须等于路径 ID。

## 响应与错误

创建和重新生成返回 `generationId`、`aiTaskId`、`agentRunId`、章节规划版本 ID 与批次状态。所有状态变动通过章节 SSE 发布 `generation.started`、`generation.updated`、`generation.failed` 或 `generation.canceled`，事件不包含正文、完整 prompt、密钥或隐藏推理。

资源不存在返回 404；非法场景选择返回 400；运行状态、版本或已固定的模型配置发生竞争/过期返回 409。调用必须使用当前可用的 Provider V2 配置；批次会持久化安全配置描述（供应商、模型、配置和凭据版本），绝不持久化 API Key 或完整 prompt。

## 人工确认边界

创建批次时由作者选择章节目标篇幅：`about_1500`、`about_3000`、`about_5000` 或 `custom`。自定义目标须在 500 到 20000 字之间。服务端按已发布场景数量确定性拆分章节目标，为每个场景给出目标值及上下浮动 10% 的字数区间，并将该区间写入生成提示词。首稿超出场景区间时会基于首稿自动执行一次扩写或压缩校正，只持久化校正后的完整候选，不截断正文。章节总字数范围是面向作者的验收口径；逐场景区间用于生成控制，不承诺每场景最终值都精确落在区间内。面向模型的 `maxOutputTokens` 仅作为宽松安全上限，再受 Provider 能力上限约束，不属于前端业务接口。

每个场景候选均固定关联场景计划版本、Story Context Snapshot、Provider/模型配置版本和 `scene-novel-v2` 模板版本。完成生成仅表示候选可预览，不等于正式正文采纳；正文写入仍经过既有的显式保存或采纳流程及其 `baseVersion` 校验。历史候选及其 `scene-novel-v1` 快照保持不变。
## 正文一致性评价

- `POST /api/chapters/{chapterId}/generations/{generationId}/evaluation-reports`：以 `idempotencyKey` 创建或复用评价，可选指定 `generationSceneId`。
- `GET /api/chapters/{chapterId}/generations/{generationId}/evaluation-reports/latest` 和按 `reportId` 查询：读取报告状态、结论、Finding 与修订候选引用。
- `POST .../{reportId}/retry`、`POST .../{reportId}/cancel`：恢复失败的语义评价步骤或取消运行中的评价。
- `GET .../{reportId}/revision-candidate`：读取局部修订候选，不代表正文已采纳。

## 场景候选读取

`GET /api/generations/{generationId}/scenes` 和 `GET /api/generations/{generationId}/scenes/{sceneId}` 返回每个场景的持久化候选正文 `generatedContent`、当前步骤尝试次数 `currentAttempt`、是否可重试 `retryable` 以及安全错误摘要 `errorCode`、`errorMessage`。

- `completed` 与 `copied` 场景的 `generatedContent` 是可预览候选，尚未采纳时不会修改章节正文。
- 仅场景状态为 `failed`、同一 `generate_scene:{sceneKey}` 的最新步骤也为失败且该步骤允许重试时，`retryable` 才为 `true`。
- `canceled` 场景不可重试；客户端只可把服务端返回的 `currentAttempt` 作为 `expectedAttempt` 提交。
- SSE 只用于即时提示；刷新、重连和失败后必须重新读取这些持久化资源。
