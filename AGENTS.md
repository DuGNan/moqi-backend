# Moqi 后端协作规范

## Java 编码规范

1. Java 代码遵守《阿里巴巴 Java 开发手册》及本项目既有 Spring Boot、Maven、多模块分层约定。
2. 接口、抽象类和实现类的方法之间必须保留一个空行；方法注解必须单独成行，不得与方法声明写在同一行。
3. 每行只写一条语句。条件、循环和方法体必须使用完整花括号，禁止把方法体或多条语句压缩到同一行。
4. 禁止使用通配符 import；必须删除未使用 import，并按 Java 标准库、第三方依赖、项目代码的顺序组织。
5. 使用四个空格缩进，不使用 Tab。代码行原则上不超过 120 个字符；长参数、链式调用和 SQL 注解应按语义换行。
6. 常量使用大写字母和下划线命名；类、方法、参数和布尔值命名遵守阿里巴巴 Java 命名规范，禁止无语义缩写。
7. Controller 只负责协议接入、参数校验和响应包装；Service 负责业务逻辑与事务；Mapper 只负责数据访问；DTO 与 Entity 不混用。
8. 格式或规范整改不得顺带改变接口契约、业务规则、事务边界、SQL 语义或数据库结构。发现行为问题时应单独说明并按 issue 流程处理。

## Lombok 使用规范

1. 持久化实体统一使用 Lombok `@Getter` 和 `@Setter` 生成机械访问器，不手写无额外逻辑的 getter/setter。
2. 持久化实体默认禁止使用 `@Data`，避免自动生成 `equals`、`hashCode` 和 `toString` 对继承关系、延迟加载或关联对象产生副作用。
3. 不使用 `@Accessors(chain = true)` 改变 JavaBean 访问约定，确保 MyBatis-Plus 属性解析保持稳定。
4. 只有在构造器、访问器或对象方法包含真实业务约束时才允许显式实现，并在代码中说明原因。

## 检查与交付

1. 修改 Java 代码后必须检查通配符 import、单行多语句、压缩方法体、超长行和方法间空行。
2. 提交前必须运行 `mvn clean test` 和 `git diff --check`。
3. 当前项目不配置自动 Checkstyle 或 PMD 门禁，规范通过 `AGENTS.md`、人工检阅和 Code Review 执行。
