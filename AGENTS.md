# Moqi 后端协作规范

本文档适用于 `E:\coding\WeWrite\moqi-backend` 独立 Git 仓库及其中的 Java、Spring Boot、Maven、数据库迁移、脚本、依赖和运行配置变更。

本文件必须能够在不读取外层目录的情况下独立约束 Codex。外层 `E:\coding\WeWrite\AGENTS.md` 是 WeWrite / Moqi 项目级规范；本文件只重复后端独立开发所必需的公共规则，并补充后端专属技术要求。

## 一、后端开发范围与规则优先级

1. 后端代码、脚本、数据库迁移、依赖和运行配置只在 `E:\coding\WeWrite\moqi-backend` 内修改。
2. 项目根目录 `E:\coding\WeWrite` 默认只承载项目级设计、规范、需求和协作文档，不得擅自将后端业务代码放入根目录。
3. 本项目采用 Java、Spring Boot、Maven、MySQL 和多模块 Maven 架构。应优先沿用现有目录、模块、依赖和实现模式。
4. 规则冲突时，按以下顺序处理：业务约定 > 安全与正确性 > 本文件中的项目强制规则 > P3C 强制规则 > 阿里推荐规则 > 个人编码偏好。
5. 不得为了通过静态检查而破坏业务语义、异常处理、事务边界、数据一致性、接口兼容性或安全性。

## 二、独立仓库必须遵守的项目协作规则

### 2.1 Issue-first

1. 所有后端代码变更必须先关联 `DuGNan/Moqi` GitHub issue。
2. 没有对应 issue 时，不得直接进入 Java 代码、数据库迁移、后端脚本、依赖或运行配置的实现；应先整理需求并补充 issue 标题、范围、验收标准和风险。
3. Issue 粒度必须是可独立交付的技术任务。不得把多个可分别交付的任务混成一个 issue，也不得把一个完整技术任务拆成过碎的零散 issue。
4. 默认一次只推进一个 issue。未经用户明确确认，不得并行推进多个后端 issue。
5. 如果实现过程中明显超出当前 issue 的独立交付边界，应先拆分或补充 issue，再继续实现。
6. 文档整理、方案讨论、纯调研、需求澄清和轻量文字修正不强制创建 issue；但只要工作会落到后端代码、脚本、迁移、依赖或运行配置，就必须遵守 issue-first。
7. 用户要求开始、实施、处理或完成一个范围明确的 issue 后，必须持续到实现与验证完成、真实阻塞或人工审查闸门；分支、worktree、代码定位和计划说明只是准备工作，不得直接作为最终交付。

### 2.2 分支、提交和合并

1. `main` 是受保护主干，不得直接在本地 `main` 上开发、提交或推送后端业务代码、配置、脚本和数据库迁移。
2. 创建工作分支时，必须先在 GitHub 远端基于最新 `main` 创建分支，再由本地执行 `git fetch` 并检出对应远端分支，建立 upstream 跟踪关系；不得先在本地创建同名分支后再补建远端分支。开始实现和提交前，必须确认当前分支不是 `main`，且其 upstream 指向对应的 `origin/<branch-name>`。
3. 代码变更分支使用以下格式：

   ```text
   <type>/<issue-number>-<short-kebab-summary>
   ```

   示例：`feature/32-p3c-report-check`、`fix/18-token-refresh`。

4. `type` 仅使用小写英文：`feature`、`fix`、`hotfix`、`refactor`、`docs`、`test`、`chore`、`ci`。
5. 分支名只使用小写英文、数字、连字符和单个 `/`；不得使用空格、中文、下划线、连续连字符或含糊缩写。
6. 涉及代码实现的分支必须包含 issue 编号；纯文档任务可以使用 `docs/<short-kebab-summary>` 或 `chore/<short-kebab-summary>`。
7. Git commit message 必须使用中文说明提交目的，可以保留必要的英文技术名词、命令名、包名、类名、接口名和错误码。
8. 完成后必须通过 GitHub Pull Request 合并回 `main`，不得通过本地直接合并或直接推送绕过 PR。
9. Pull Request 至少需要一次 Code Review approval。PR 标题、正文、变更摘要、测试说明、review 回复和 merge 说明默认使用中文。

### 2.3 提交前人工审查闸门

1. 完成实现和必要验证后，Codex 必须先向用户展示待提交文件清单、`git diff --stat`、变更摘要和验证结果，然后暂停等待审查；不得直接执行 `git add`、`git commit`、`git push` 或创建 Pull Request。
2. 用户可以在本地 IDE 中人工查看改动，也可以明确要求使用代码审查 Agent，或同时执行两种审查。Codex 应提供当前工作分支、仓库路径和待审查文件，方便用户定位改动。
3. 审查发现问题时，Codex 必须先完成修正和必要验证，再重新展示待提交文件与 diff，并再次暂停；上一轮的审查结论不得自动沿用。
4. 只有用户在当前审查轮次中明确回复“批准提交”或表达同等明确的提交授权后，Codex 才能执行 `git add`、`git commit` 和 `git push`。“继续”“处理 issue”“完成任务”等泛化指令不视为提交授权。
5. commit 和 push 完成后，Codex 必须报告提交哈希、远端分支和相对 `main` 的提交范围，并再次等待用户确认；只有用户明确批准创建 Pull Request 后，才能创建 PR。
6. 本节是后端代码、配置、脚本、数据库迁移、依赖和重要项目文档变更的强制流程。用户在当前任务中明确要求跳过某一闸门时，Codex 必须说明将跳过的步骤和影响，并仅按该次授权执行，不得把例外延续到后续任务。
7. 用户在当前轮次已经明确要求提交、推送并创建 Pull Request 时，视为一次性批准本轮完整发布链路；不得在 commit、push 和 PR 之间重复请求同一授权。

### 2.4 中文协作

1. Issue 标题、正文、验收标准、评论和补充说明使用中文。
2. Pull Request 标题、正文、变更摘要、测试说明、review 回复和合并说明使用中文。
3. 交付说明、项目文档、接口说明、测试说明和维护记录使用中文。
4. 代码标识符、文件名、依赖名、脚本命令、API path、类型名和枚举值按技术约定使用英文，不受中文协作规则限制。
5. 外部工具生成英文模板时，提交前应改写为中文；无法改写的内容必须在交付说明中明确标出。

### 2.5 安装、迁移和目录边界

1. 涉及下载、安装、迁移任何中间件、数据库、运行时或第三方服务前，必须先询问用户安装路径并得到确认。
2. 未经用户确认，不得将下载类中间件安装到系统盘或隐藏目录。
3. 当前本地 MySQL 路径基线为 `E:\middleware\mysql-8.4.9`。这只是当前环境基线，不代表可以未经确认执行安装、迁移或配置变更。
4. 后端目录外的文件、数据库实例、服务进程和系统配置不属于默认修改范围；需要扩大范围时，必须先获得用户确认。
5. 后端代码变更必须同步维护 `docs/项目结构与功能介绍.md`，确保目录结构、模块职责、接口行为、数据库要点、运行方式和已知边界保持当前有效。

## 三、编码前工作流程

开始编码前必须：

1. 阅读相关模块目录、接口、实现、配置、数据库迁移和测试。
2. 搜索同类功能，优先复用项目已有模式，不在未理解现有代码前新建 `Util`、`Manager`、`Helper`、基础类、异常类或设计模式。
3. 确认当前 Git 仓库、分支、issue、影响模块和文件范围。
4. 明确本次变更对 API、DTO、Entity、Mapper、数据库、事务、日志、异常和测试的影响。
5. 说明实现方案、可能触发的 P3C 规则和验证范围。
6. 需求、接口、数据约束或异常行为不明确时，先提出问题，不猜测实现。

## 四、项目架构与职责边界

1. `Controller` 只负责 HTTP 协议适配、参数校验、认证上下文获取和响应包装，不承载核心业务逻辑、数据库访问或事务编排。
2. `Service` 负责业务规则、业务编排、事务边界和领域校验；复杂方法应拆分为职责清晰的私有方法或独立服务。
3. `Mapper` / `Repository` 只负责数据访问、查询和持久化，不承载跨聚合业务决策或远程调用。
4. `DTO` 用于接口输入输出或模块间传输，`Entity` 用于持久化映射；不得把 Entity 直接作为公共 API 契约，DTO 与 Entity 不混用。
5. 数据库迁移、Entity、Mapper、Service、Controller、测试、接口文档和项目介绍文档必须保持同步。
6. 不得为减少代码行数而合并层次、复用错误的对象类型或牺牲可读性。
7. 不得随意新增全局配置、拦截器、切面、自动装配、线程池或公共基础设施；新增前应说明必要性、影响范围和关闭方式。

## 五、Java 编码规范

### 5.1 命名与格式

1. 类名使用 PascalCase；方法名、变量名和参数名使用 camelCase；常量使用全大写加下划线。
2. 布尔变量和方法使用具有语义的 `is`、`has`、`can`、`should` 等前缀。
3. 禁止使用无意义名称，例如 `data`、`obj`、`temp`、`flag`，除非上下文能够明确表达含义。
4. 禁止使用拼音、中文或拼音与英文混合命名；不得以下划线或美元符号作为名称的开头或结尾。
5. 每行只写一条语句；条件、循环和方法体必须使用完整花括号，不得压缩为单行方法体。
6. 接口、抽象类和实现类的方法之间保留空行；方法注解单独成行，不得与方法声明写在同一行。
7. 使用四个空格缩进，不使用 Tab；原则上每行不超过 120 个字符。长参数、链式调用和 SQL 应按语义换行。
8. 禁止通配符 `import`；删除未使用的 import，并按 Java 标准库、第三方依赖、项目代码的顺序组织 import。
9. 不得通过格式化顺带修改接口契约、业务规则、事务边界、SQL 语义或数据库结构。

### 5.2 Java 类型注释

1. 新增生产代码和测试代码的 Java 顶层类型必须添加类型级 Javadoc，放在 import 区域之后、类型声明之前：

   ```java
   /**
    * @author dgn
    * @date 2026-01-01
    * @description 类型的实际职责
    */
   ```

2. `@author` 默认使用 `dgn`；除非用户明确指定其他创建者，不得擅自修改。
3. `@date` 使用创建文件时的本地日期，格式为 `yyyy-MM-dd`，不得保留 `${YEAR}`、`${MONTH}`、`${DAY}` 占位符。
4. `@description` 必须使用简洁明确的中文描述类型实际职责，不得留空，也不得只重复类名或使用“工具类”“处理业务”等含糊表述。
5. 类型职责发生实质变化时同步更新 `@description`；仅修改实现细节时不修改创建日期。
6. 阅读或修改既有 Java 文件时，同时检查类型注释位置、内容、日期格式和职责描述是否与当前实现一致。

## 六、Lombok 使用规范

1. 持久化 Entity 按项目现有约定使用 Lombok `@Data` 生成常规 getter、setter、`equals`、`hashCode` 和 `toString`，不得重复声明无额外业务逻辑的访问器。
2. Entity 存在继承关系时，必须明确评估 `equals`、`hashCode` 和 `toString` 是否应包含父类字段；不得为了消除编译警告盲目修改行为。
3. 不使用 `@Accessors(chain = true)` 改变既有 JavaBean 访问约定，确保 MyBatis-Plus 和序列化行为稳定。
4. 只有在构造器、访问器或对象方法包含真实业务约束时，才允许手写实现，并在代码或交付说明中说明原因。
5. 不得混用重复的 `@Data`、`@Getter`、`@Setter` 或手写访问器；如确有必要，必须说明生成行为和覆盖关系。

## 七、异常、日志与空值

1. 禁止捕获异常后不处理、吞掉堆栈或直接忽略；异常应尽量使用具体类型，不以 `Exception` 或 `Throwable` 代替明确异常类型。
2. 重新抛出异常时保留原始 cause，不得丢失堆栈信息；异常消息应包含必要业务上下文，但不得泄露密码、Token、密钥、身份证号等敏感信息。
3. 不得使用异常控制正常业务流程；业务异常和系统异常必须保持清晰区分。
4. Controller、Service、Mapper 多层不得重复记录同一异常；通常在能够补充完整上下文的边界记录一次。
5. 使用项目统一日志框架，禁止使用 `System.out.println`、`System.err.println` 或临时控制台输出替代日志。
6. 日志不得输出密码、Token、密钥、完整身份证号、完整手机号等敏感信息；必要时使用脱敏值。
7. 日志应包含定位问题所需的业务标识和上下文，避免直接拼接复杂对象、大文本或用户输入。
8. 明确区分 `null`、空集合和空字符串的业务含义；返回集合时，除非业务明确要求，否则优先返回空集合而不是 `null`。
9. 使用对象、集合、Optional 或外部输入前必须确认空值处理方式，不得依赖偶然的非空状态。

## 八、事务、数据库和 SQL

1. 事务边界必须与业务一致，通常由 Service 层负责；不得在 Controller 或 Mapper 层随意开启事务。
2. 事务方法必须根据异常语义明确配置回滚策略，必要时使用 `rollbackFor`；不得为了消除 P3C 提示机械添加事务。
3. 事务内不得执行不必要的远程调用、文件传输或长时间阻塞操作。
4. 禁止直接拼接用户输入形成 SQL；动态条件、排序和分页必须经过白名单或参数化处理。
5. 新增或修改 SQL 时检查索引、分页、排序、空值行为、数据权限、返回字段和可能的全表扫描。
6. 禁止在循环中逐条查询或写入数据库，除非已确认数据量、事务边界和性能可接受；优先使用批量查询或批量写入。
7. 修改数据库结构时必须同步更新迁移脚本、Entity、Mapper、Service、测试、接口文档和项目介绍文档。
8. 数据库迁移脚本必须可重复审查，明确版本、执行顺序、字段含义、索引、约束和回滚风险；不得直接修改已执行迁移脚本而不说明影响。

## 九、并发与资源管理

1. 共享可变状态必须明确线程安全策略；不得使用非线程安全集合承载跨线程共享数据。
2. 禁止随意创建线程或线程池。使用线程池时必须说明核心线程数、最大线程数、队列容量、拒绝策略、任务超时和关闭方式。
3. 避免锁粒度过大、锁顺序不一致和在锁内执行远程调用；不得通过简单加锁掩盖状态设计问题。
4. 外部连接、文件、流、线程池和其他资源必须在明确的生命周期内创建、使用和释放。
5. 不得在循环中重复创建不必要的对象、执行不必要的远程调用或访问数据库。

## 十、P3C 与验证

1. 本项目通过后端 `pom.xml` 接入 Alibaba P3C PMD 规则，并在 Maven `verify` 阶段以报告模式执行；`failOnViolation` 必须保持为 `false`，使规范违规能够输出提示和报告，但不因违规阻断编译、测试、验证或打包。
2. Maven 构建成功不代表 P3C 规范全部通过。必须分别报告 Maven 构建测试结果、P3C 检查结果和遗留规范问题。
3. 修改 Java 代码后，至少人工检查类型注释、Lombok 注解、通配符 import、单行多语句、压缩方法体、超长行、方法间空行、异常、日志、空值、事务、SQL、并发和 API 兼容性。
4. 提交前必须执行：

   ```bash
   mvn clean verify
   git diff --check
   ```

5. `mvn clean verify` 应实际触发 P3C 检查并输出结果；必要时根据受影响范围补充执行模块测试、集成测试、数据库迁移验证和接口测试。
6. 未实际执行验证命令时，不得声称构建、测试、P3C 检查或规范检查通过。
7. 验证失败时，必须说明失败命令、失败原因、是否由本次变更引入以及后续处理方式。
8. 不得通过删除规则、关闭检查、批量增加 `@SuppressWarnings` 或修改规则集来掩盖本次变更引入的问题。

## 十一、AI 修改流程与最终交付

每次后端修改必须遵循：

1. 先说明需求理解、issue、影响范围和实现方案。
2. 先阅读相关代码、配置、迁移和测试，再开始修改。
3. 优先做最小范围修改，不进行无关重构。
4. 编码后检查架构边界、API 兼容性、异常、日志、空值、事务、SQL、并发、P3C 和测试。
5. 同步更新 `docs/项目结构与功能介绍.md`，除非本次变更确实不会改变后来者理解项目所需的信息。
6. 没有文件变更、工具调用或验证输出作为证据时，不得声称正在修改、已经实现或已经修复；执行中的进度使用 commentary，final 只用于完成、真实阻塞、人工闸门或必须由用户决策的事项。
7. 最终交付说明必须分别报告：
   - 修改了什么；
   - 修改了哪些文件；
   - 关联的 GitHub issue 和分支；
   - 执行了哪些 Maven 构建和测试；
   - 执行了哪些 P3C 或其他规范检查；
   - Maven 构建测试结果；
   - P3C 检查结果；
   - 尚未解决的问题、兼容性风险或环境限制。
8. 如果确实需要违反规范，必须说明违反哪条规则、为什么必须违反、影响是什么，以及是否需要增加抑制配置或后续治理任务。
